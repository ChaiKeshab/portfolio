---
const experiences = [
  {
    title: "React.js Developer",
    company: "AusNep IT Solution Pvt. Ltd.",
    duration: "Oct 2023 - Present",
    description: [
      "Architected and rebuilt core systems where legacy solutions were unscalable, introducing TanStack Query to replace fragile Redux Observable/Thunk setups and migrating projects from JavaScript to strict TypeScript",
      "Built a Google Drive–like file system from scratch and collaborated with 2 backend engineers to design a scalable e-commerce platform that evolved from single-tenant to multi-tenant with unlimited storefronts",
      "Implemented real-time socket systems (STOMP/WebSocket) for live chat and notifications, partnered with backend to design secure Stripe payments flow, and developed responsive CRM/Dashboard with persistent menu states",
      "Improved frontend security by auditing source map leakage, refactored service-listing feature for shareable URLs, and researched/prototyped OpenAI voice agent MVP using Whisper-1, GPT-4o-mini, and TTS-1",
    ],
  },
];
---

<section id="experience" class="relative min-h-screen flex items-center py-20">
  <div class="container mx-auto px-2 md:px-12 lg:px-4">
    <div class="max-w-4xl">
      <h2
        class="flex items-center gap-2 text-2xl md:text-3xl font-bold text-text mb-12"
      >
        <span class="font-mono text-primary">02.</span>
        Where I've Worked
      </h2>

      <div class="relative md:border-l md:ml-4 border-primary/30">
        {
          experiences.map((exp) => (
            <div class="mb-12 md:ml-8">
              <span class="absolute hidden md:flex items-center justify-center w-6 h-6 rounded-full -left-3 ring-8 ring-background bg-primary">
                <span class="w-3 h-3 rounded-full bg-background" />
              </span>
              <div class="space-y-3">
                <div class="flex flex-wrap items-center gap-x-2">
                  <h3 class="text-xl font-semibold text-text">
                    {exp.title}
                    <span class="text-primary"> @ {exp.company}</span>
                  </h3>
                </div>
                <span class="block font-mono text-sm text-muted">
                  {exp.duration}
                </span>
                <ul class="space-y-2">
                  {exp.description.map((point) => (
                    <li class="flex items-start gap-4 text-muted">
                      <span class="text-primary text-xl">▹</span>
                      <span>{point}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
