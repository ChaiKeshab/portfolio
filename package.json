{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm run build:resume && astro dev --host",
    "build:resume": "npx typst-cli compile resume/myResumeName.typ public/keshabChaiResume.pdf",
    "build": "pnpm run build:resume && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "astro": "^5.11.0",
    "astro-icon": "^1.1.5",
    "sharp": "^0.34.3",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "typst-cli": "^1.0.0"
  }
}